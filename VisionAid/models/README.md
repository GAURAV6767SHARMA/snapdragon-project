Place the exported/quantized detection model here as `yolov8n_int8.onnx`.

See `../docs/model_export_guide.md` for how to get it from Qualcomm AI Hub
(recommended) or export it manually with Ultralytics + ONNX Runtime
quantization.
